import 'package:flutter/widgets.dart';

class DetailConsentFormScreen extends StatelessWidget {
  const DetailConsentFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}